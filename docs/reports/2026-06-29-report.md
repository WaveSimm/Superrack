# 작업 보고서 — 2026-06-29

> 프로젝트: Superrack (ASIO 다채널 VST3 인서트 프로세서) · 워크플로우: bkit/PDCA

## 오늘 완료한 것

### PDCA 구조 세팅
- `docs/01-plan/PLAN.md`, `docs/02-design/DESIGN.md` 정규화, `.bkit` feature 등록(`asio-vst3-host`)

### Phase 0 — GUI 골격 + 2ch 1:1 패스스루 ✅ 실기 검증
- CMake + JUCE 8.0.11 FetchContent, `AudioEngine`(ASIO 콜백, 락프리 peak 미터), `MainComponent`(장치 패널 + 미터)
- UR22mkII ASIO 48k/64smp 패스스루·미터 동작 확인

### Phase 1 — 채널별 VST3 체인 ✅ 실기 검증
- `ChannelStrip`(VST3 직렬 체인, 모노→스테레오 처리, lock-free 재구성), `PluginWindow`(네이티브 에디터)
- 채널별 추가/Bypass/제거/에디터, FabFilter Pro-Q 3 체인 동작 확인

### 부가 — 루프백 레이턴시 테스터 ✅
- Out1→In2 임펄스 실측 RTL vs 드라이버 보고 비교. 측정 결과: `docs/measurements/latency-ur22mkii.md`
- 핵심 발견: 고정 컨버터 지연 ≈ 123~124 smp(2.56ms), 버퍼 무관

### 설정 영속화 ✅
- `AudioDeviceManager` 상태를 `%APPDATA%/Superrack/audio-settings.xml` 에 저장/복원

## 빌드 중 잡은 이슈 (PDCA Check)
1. **JUCE 8.0.11 API**: `addDefaultFormats()` 삭제 → `addDefaultFormatsToManager()`
2. **자기 파괴 크래시**: 제거 버튼 콜백 내 행 삭제 → `callAsync` 지연
3. **한글 인코딩**: `juce::String(const char*)` 는 ASCII 전용 → 모든 한글 리터럴 `CharPointer_UTF8`(`sr::u8`) 래핑 + Malgun Gothic 폰트
4. `juce_audio_utils` 헤더 누락, `/utf-8`, Font/shadowing 경고 정리 → clean 빌드

## 상태
- 빌드: MSVC Release, 경고/에러 0
- 실행 파일: `build/Superrack_artefacts/Release/Superrack.exe`

## 다음 (미완)
- **Phase 2**: 채널별 Dry 멀티트랙 녹음 (락프리 FIFO + ThreadedWriter)
- Phase 3: 출력 게인·xrun·세션(플러그인 체인) 저장/로드
- Phase 4: 32채널 확장 + CPU 프로파일링
- (선택) 레이턴시 테스터 고도화: 다중 평균, 임의 채널 선택
